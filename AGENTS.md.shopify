# Pi Configuration for Shopify Development

This file provides context for pi when working in a Shopify monorepo.

## Project Context

- Large monorepo requiring sparse checkout
- Uses standard git + gh CLI for PR management
- Uses `dev` command for server management
- Worktrees managed via WTP (`_wtp`)

## Workflow Preferences

### PR Reviews
When user asks to review a PR, proactively:
1. Suggest using the `review` skill
2. Remind them about sparse checkout if needed
3. Show PR description and context

### New Features
When starting new work:
1. Use WTP for worktree management (`_wtp`)
2. Create feature branches with `git checkout -b <branch>`
3. Follow conventional commit format
4. Push with `git push -u origin <branch>` and `gh pr create --draft`

### Common Commands
- `git log --oneline` — view recent commits
- `git push` — push changes
- `gh pr create --draft` — create draft PR
- `gh pr ready <N>` — mark PR ready for review
- `devx ci run` — trigger CI (doesn't auto-run on push)

### Server Management
- Only one `dev up` server at a time
- Always `dev down` before switching worktrees

## Code Style

- Ruby: Follow Rubocop configuration
- JavaScript/TypeScript: Follow ESLint configuration
- Commit messages: Conventional commits (feat:, fix:, chore:, etc.)

## Testing

Before submitting PRs:
- Run relevant tests: `dev test`
- Check linting: `dev style`
- Verify in browser if UI changes

## Common Issues

1. **Sparse checkout missing files**: Run `git sparse-checkout reapply`
2. **Worktree conflicts**: Use `git worktree remove --force`
3. **Server port in use**: Find and kill with `killport 3000`
