# Orphan cleanup — shop-pi-fy symlinks not in new manifest
#
# These are symlinks in ~/.pi/agent/ that currently resolve into
# the shop-pi-fy package directory but are NOT in shop-pi-fy.installs.
# Run interactively — paste lines you want into pi.
#
# Generated 2026-05-06

## Extensions (25 orphans)

# Unchecked — user chose not to include
/pkg rm agent-device
/pkg rm cloudsmith
/pkg rm co-authored-by-ai
/pkg rm deep-review
/pkg rm fellow
/pkg rm image-paste
/pkg rm investigator
/pkg rm kafkanet
/pkg rm kubernetes
/pkg rm migration-tracker
/pkg rm no-sleep-while-working
/pkg rm powerline-footer
/pkg rm pr-joust
/pkg rm save-to-md
/pkg rm scout
/pkg rm shopp-e
/pkg rm shoulderpeek
/pkg rm slack-tui
/pkg rm spy
/pkg rm superpowers
/pkg rm tree
/pkg rm vscode-bridge
/pkg rm yugabyte-dashboard

# Fork conflicts — shop-pi-fy version should not coexist with dotfiles fork
/pkg rm file-preview
/pkg rm guard

## Skills (1 orphan)

/pkg rm code-review

## Agents (5 orphans)

/pkg rm code-review-tracer
/pkg rm dw-infra-heuristics
/pkg rm dw-infra-playbooks
/pkg rm dw-infra-run-validate-model
/pkg rm dw-infra-sql

## Prompts (0 orphans)

# None — all current prompt symlinks are in the manifest.
